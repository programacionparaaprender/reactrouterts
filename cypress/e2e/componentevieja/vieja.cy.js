describe('Navegaciones', ()=>{
    it("visita juego de la vieja", ()=>{
        // eslint-disable-next-line no-undef
        cy.visit("http://localhost:3000/reactrouterts");
        // eslint-disable-next-line no-undef
        //cy.get("menu").contains("vieja").click();
        // eslint-disable-next-line no-undef
        //cy.get("menu").contains("googlemaps").click();
        // eslint-disable-next-line no-undef
        //cy.get("menu").contains("counter").click();
        // eslint-disable-next-line no-undef
        //cy.get("menu").contains("fetch-data").click();
        // eslint-disable-next-line no-undef
        //cy.get("menu").contains("product").click();
    });
});